#include <iostream>
#include <stdio.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
int main(int argc, char** argv) {
	/*int x,y,change;
	printf("How much do you have");
	
	scanf("%d", &x);
	printf("Hpow much do you want to use to purchase");
	scanf("%d", &y);
     
    change;
	printf("The balance is  %d", change);
	return 0;*/
	
	float number = 10;
	float digit = 15;
	float x = number + digit;
	printf("%2f", x);mki
}
